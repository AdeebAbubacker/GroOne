import 'package:flutter/material.dart';

class ActiveTrips extends StatelessWidget {
  const ActiveTrips({super.key});

  @override
  Widget build(BuildContext context) {
    return    Center(child: Text('Active Trips'));
  }
}
